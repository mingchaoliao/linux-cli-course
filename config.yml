title: linux-cli
description: >-
  To understand basic UNIX-like commands and be able to perform simple tasks in
  the terminal.
template:
  repo: linux-cli-template
  name: linux-cli
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
